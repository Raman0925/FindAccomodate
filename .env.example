# Copy to `.env.local` (gitignored) at the repo root.
# After editing, restart Metro: npx react-native start --reset-cache

PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
PUBLIC_SUPABASE_KEY=your_publishable_or_anon_key
